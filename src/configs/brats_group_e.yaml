model:
  in_channels: 4
  out_channels: 1
  channels: [32, 64, 128, 256]

train:
  batch_size: 1
  lr: 0.0001
  seed: [0]
  crop_size: [96, 96, 96]

data:
  dim: 3d
  batch_size: 1
  num_workers: 4
  crop_size: [96, 96, 96]

log:
  out_dir: runs/brats_group_e
  ckpt_path: runs/brats_group_e/seed_0/best.pt